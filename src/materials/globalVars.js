let mapContainer = {
        el: document.getElementById("mapContainer")
    },
    map = {
        el: document.getElementById("map")
    },
    mapDimensions = 500,
    gridPartDimensions = 20,
    gridPartSize = mapDimensions / gridPartDimensions